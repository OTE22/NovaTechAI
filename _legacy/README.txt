Superseded files from the previous version of the NovaTechAI website.

  styles.css           -> replaced by assets/css/site.css
  blog.css             -> replaced by assets/css/editorial.css
  script.js            -> replaced by assets/js/site.js
  novatechai-hero.png  -> removed; the hero is now built from CSS and inline SVG
                          (the PNG was 1.77 MB and was the largest-contentful-paint
                          element on mobile)

Nothing in this folder is referenced by the live site. EXCLUDE IT FROM DEPLOYMENT
(see AWS_DEPLOYMENT.md) or delete the whole folder once you are happy with the
new site.
